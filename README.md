# dijkstra_algo_simulator
Dijkstra’s algorithm is a single-source shortest path algorithm designed for weighted graphs with non-negative edge weights.  Published by Edsger Dijkstra in 1959, it finds the minimum distance from a starting node (source) to all other nodes in the graph, effectively building a shortest-path tree. 
